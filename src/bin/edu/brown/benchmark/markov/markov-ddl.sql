CREATE TABLE TABLEA (
   A_ID BIGINT NOT NULL,
   A_SATTR00  VARCHAR(32),
   A_SATTR01  VARCHAR(32),
   A_SATTR02  VARCHAR(32),
   A_SATTR03  VARCHAR(32),
   A_SATTR04  VARCHAR(32),
   A_SATTR05  VARCHAR(32),
   A_SATTR06  VARCHAR(32),
   A_SATTR07  VARCHAR(32),
   A_SATTR08  VARCHAR(32),
   A_SATTR09  VARCHAR(32),
   A_SATTR10  VARCHAR(32),
   A_SATTR11  VARCHAR(32),
   A_SATTR12  VARCHAR(32),
   A_SATTR13  VARCHAR(32),
   A_SATTR14  VARCHAR(32),
   A_SATTR15  VARCHAR(32),
   A_SATTR16  VARCHAR(32),
   A_SATTR17  VARCHAR(32),
   A_SATTR18  VARCHAR(32),
   A_SATTR19  VARCHAR(32),
   A_IATTR00  INTEGER,
   A_IATTR01  INTEGER,
   A_IATTR02  INTEGER,
   A_IATTR03  INTEGER,
   A_IATTR04  INTEGER,
   A_IATTR05  INTEGER,
   A_IATTR06  INTEGER,
   A_IATTR07  INTEGER,
   A_IATTR08  INTEGER,
   A_IATTR09  INTEGER,
   A_IATTR10  INTEGER,
   A_IATTR11  INTEGER,
   A_IATTR12  INTEGER,
   A_IATTR13  INTEGER,
   A_IATTR14  INTEGER,
   A_IATTR15  INTEGER,
   A_IATTR16  INTEGER,
   A_IATTR17  INTEGER,
   A_IATTR18  INTEGER,
   A_IATTR19  INTEGER,
   PRIMARY KEY (A_ID)
);

CREATE TABLE TABLEB (
   B_ID BIGINT NOT NULL,
   B_A_ID BIGINT NOT NULL REFERENCES TABLEA (A_ID),
   B_SATTR00  VARCHAR(32),
   B_SATTR01  VARCHAR(32),
   B_SATTR02  VARCHAR(32),
   B_SATTR03  VARCHAR(32),
   B_SATTR04  VARCHAR(32),
   B_SATTR05  VARCHAR(32),
   B_SATTR06  VARCHAR(32),
   B_SATTR07  VARCHAR(32),
   B_SATTR08  VARCHAR(32),
   B_SATTR09  VARCHAR(32),
   B_SATTR10  VARCHAR(32),
   B_SATTR11  VARCHAR(32),
   B_SATTR12  VARCHAR(32),
   B_SATTR13  VARCHAR(32),
   B_SATTR14  VARCHAR(32),
   B_SATTR15  VARCHAR(32),
   B_IATTR00  INTEGER,
   B_IATTR01  INTEGER,
   B_IATTR02  INTEGER,
   B_IATTR03  INTEGER,
   B_IATTR04  INTEGER,
   B_IATTR05  INTEGER,
   B_IATTR06  INTEGER,
   B_IATTR07  INTEGER,
   B_IATTR08  INTEGER,
   B_IATTR09  INTEGER,
   B_IATTR10  INTEGER,
   B_IATTR11  INTEGER,
   B_IATTR12  INTEGER,
   B_IATTR13  INTEGER,
   B_IATTR14  INTEGER,
   B_IATTR15  INTEGER,
   PRIMARY KEY (B_ID, B_A_ID)
);

CREATE TABLE TABLEC (
   C_ID BIGINT NOT NULL,
   C_A_ID BIGINT NOT NULL REFERENCES TABLEA (A_ID),
   C_SATTR00  VARCHAR(128),
   C_SATTR01  VARCHAR(128),
   C_SATTR02  VARCHAR(128),
   C_SATTR03  VARCHAR(128),
   C_SATTR04  VARCHAR(128),
   C_SATTR05  VARCHAR(128),
   C_SATTR06  VARCHAR(128),
   C_SATTR07  VARCHAR(128),
   C_SATTR08  VARCHAR(128),
   C_SATTR09  VARCHAR(128),
   C_SATTR10  VARCHAR(128),
   C_SATTR11  VARCHAR(128),
   C_SATTR12  VARCHAR(128),
   C_SATTR13  VARCHAR(128),
   C_SATTR14  VARCHAR(128),
   C_SATTR15  VARCHAR(128),
   C_IATTR00  INTEGER,
   C_IATTR01  INTEGER,
   C_IATTR02  INTEGER,
   C_IATTR03  INTEGER,
   C_IATTR04  INTEGER,
   C_IATTR05  INTEGER,
   C_IATTR06  INTEGER,
   C_IATTR07  INTEGER,
   C_IATTR08  INTEGER,
   C_IATTR09  INTEGER,
   C_IATTR10  INTEGER,
   C_IATTR11  INTEGER,
   C_IATTR12  INTEGER,
   C_IATTR13  INTEGER,
   C_IATTR14  INTEGER,
   C_IATTR15  INTEGER,
   PRIMARY KEY (C_ID, C_A_ID)
);

CREATE TABLE TABLED (
   D_ID BIGINT NOT NULL,
   D_B_ID BIGINT NOT NULL,
   D_B_A_ID BIGINT NOT NULL,
   D_C_ID BIGINT NOT NULL,
   D_C_A_ID BIGINT NOT NULL,
   D_SATTR00  VARCHAR(32),
   D_SATTR01  VARCHAR(32),
   D_SATTR02  VARCHAR(32),
   D_SATTR03  VARCHAR(32),
   D_SATTR04  VARCHAR(32),
   D_SATTR05  VARCHAR(32),
   D_SATTR06  VARCHAR(32),
   D_SATTR07  VARCHAR(32),
   D_SATTR08  VARCHAR(32),
   D_SATTR09  VARCHAR(32),
   D_SATTR10  VARCHAR(32),
   D_SATTR11  VARCHAR(32),
   D_SATTR12  VARCHAR(32),
   D_SATTR13  VARCHAR(32),
   D_SATTR14  VARCHAR(32),
   D_SATTR15  VARCHAR(32),
   D_IATTR00  INTEGER,
   D_IATTR01  INTEGER,
   D_IATTR02  INTEGER,
   D_IATTR03  INTEGER,
   D_IATTR04  INTEGER,
   D_IATTR05  INTEGER,
   D_IATTR06  INTEGER,
   D_IATTR07  INTEGER,
   D_IATTR08  INTEGER,
   D_IATTR09  INTEGER,
   D_IATTR10  INTEGER,
   D_IATTR11  INTEGER,
   D_IATTR12  INTEGER,
   D_IATTR13  INTEGER,
   D_IATTR14  INTEGER,
   D_IATTR15  INTEGER,
   FOREIGN KEY (D_B_ID, D_B_A_ID) REFERENCES TABLEB (B_ID, B_A_ID),
   FOREIGN KEY (D_C_ID, D_C_A_ID) REFERENCES TABLEC (C_ID, C_A_ID),
   PRIMARY KEY (D_ID)
);