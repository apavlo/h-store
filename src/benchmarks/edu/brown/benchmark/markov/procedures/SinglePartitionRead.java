package edu.brown.benchmark.markov.procedures;

import org.voltdb.VoltProcedure;
import org.voltdb.VoltTable;

public class SinglePartitionRead extends VoltProcedure {

    public VoltTable[] run(long a_id, long value) {

        return (null);
    }
}
