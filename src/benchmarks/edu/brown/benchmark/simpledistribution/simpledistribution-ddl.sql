CREATE TABLE TABLEA (
   A_ID     BIGINT NOT NULL,
   A_VALUE  VARCHAR(64)
);

CREATE TABLE TABLEB (
   B_ID	    BIGINT NOT NULL,
   NUMROWS  INT    NOT NULL
);

CREATE STREAM S1
(
  value            integer     NOT NULL
);

CREATE STREAM S2
(
  value            integer     NOT NULL
);

CREATE STREAM S3
(
  value            integer     NOT NULL
);

CREATE STREAM S4
(
  value            integer     NOT NULL
);

CREATE STREAM S5
(
  value            integer     NOT NULL
);

CREATE STREAM S6
(
  value            integer     NOT NULL
);




