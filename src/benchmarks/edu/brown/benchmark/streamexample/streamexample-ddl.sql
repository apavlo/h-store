CREATE TABLE TABLEB (
   B_ID	    BIGINT NOT NULL,
   NUMROWS  INT    NOT NULL,
   PRIMARY KEY (B_ID)
);

CREATE TABLE TABLEC (
   A_ID     BIGINT NOT NULL,
   A_VALUE  VARCHAR(64),
   PRIMARY KEY (A_ID)
);

CREATE TABLE TABLEA (
   A_ID     BIGINT NOT NULL,
   A_VALUE  VARCHAR(64),
   PRIMARY KEY (A_ID)
);

